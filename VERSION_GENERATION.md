# Version Generation

The `version.libsonnet` file is auto-generated and should be committed to the repository.

When developing locally, you can run:

```bash
./generate-version.sh
```


The `generate-version.sh` script creates `version.libsonnet` with the current Git SHA and tag information